name: visa_app
description: Aplicación de asistencia para solicitudes de visa.
publish_to: 'none'

version: 1.0.0+7

environment:
  sdk: ^3.11.1

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8

  # Firebase
  firebase_core: ^4.0.0
  firebase_auth: ^6.0.0
  cloud_firestore: ^6.0.0
  firebase_storage: ^13.0.0
  firebase_messaging: ^16.0.0

  # Google Sign In
  google_sign_in: ^6.2.1

  # Estado
  provider: ^6.1.5

  # Archivos e imágenes
  image_picker: ^1.2.0
  file_picker: ^10.3.2
  url_launcher: ^6.3.1

  # Utilidades
  intl: ^0.20.2
  shared_preferences: ^2.5.3
  uuid: ^4.5.1
  flutter_svg: ^2.2.0
  cached_network_image: ^3.4.1
  flutter_local_notifications: ^22.3.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^6.0.0

  flutter_launcher_icons: ^0.14.4

flutter_launcher_icons:
  android: true
  ios: true
  image_path: "assets/images/app_icon.png"
  min_sdk_android: 21

flutter:
  uses-material-design: true

  assets:
    - assets/images/
    - assets/icons/